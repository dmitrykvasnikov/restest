# Local configuration, which holds the database password.
.env

# Build output.
/bin/

# Go test and coverage artefacts.
*.out
*.test

# Database dumps taken by scripts/backup.sh. A backup holds every account's
# data and belongs wherever backups are kept, not in the repository.
/backups/
